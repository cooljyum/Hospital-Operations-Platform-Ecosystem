# CLAUDE.md

## 작업 검증 정책

- **단순 수치 변경**(상수·설정값·타임아웃·임계치 등 단일 값 수정처럼 부작용이 자명한 변경)은 바로 진행한다.
- **그 외 모든 작업**(로직 변경, 신규 기능, 리팩터링, 여러 파일에 걸친 수정 등)은 `docs/agents/gemini.md`에 정의된 Gemini CLI 검증 파이프라인을 따른다. 즉:
  - step으로 쪼개 진행하고, 완수 여부를 스스로 판단하지 않는다.
  - 각 step은 `docs/agents/gemini.md` §2~§4의 호출 방식·프롬프트 템플릿·진행 루프대로 Gemini CLI(`gemini`)에 위임해 `VERDICT: PASS|FAIL`을 받는다.
  - PASS 전까지 해당 step을 완료로 보고하지 않는다.
- 작업을 시작하기 전 `docs/agents/gemini.md`를 읽고 그 안의 절차(§1 사전조건 확인 ~ §6 예시)를 그대로 따른다.

## 디자인 작업 정책

- 화면·컴포넌트·스타일 등 **디자인 관련 작업**(UI 구현, 색상·타이포·레이아웃 결정, 배지·라벨·아이콘 등 컴포넌트 추가)을 할 때는 반드시 `docs/DESIGN.md`를 먼저 읽고 그 안의 컬러·타이포그래피·컴포넌트 스펙과 Do's/Don'ts를 따른다.
- 특히 `docs/DESIGN.md`에 명시된 다음 규칙은 예외 없이 지킨다: 배지·태그·상태 라벨은 항상 `{colors.surface}` 중립 배경을 유지하고 색상은 텍스트로만 표현한다(색 채움 칩 금지), 이모지는 아이콘·불릿·본문 어디에도 쓰지 않는다(대신 SVG 아이콘 사용).

## 레이아웃 기준

운영 콘솔(EMR/HIS 화면) UI를 만들 때는 `docs/design/reference-layout-mediflow-emr.png`(MediFlow EMR 대시보드 스크린샷)를 **레이아웃 기준점**으로 삼는다. 색상·폰트 등 비주얼 톤은 `docs/DESIGN.md`(Notion 스타일)를 따르되, **화면 구조·정보 배치는 이 레퍼런스의 골격을 그대로 따른다**:

- **최상단 헤더바** (전체 폭, 흰 배경, 하단 헤어라인 구분): 좌측 로고+워드마크 / 중앙 전역 검색(단축키 힌트 포함) / 우측에 요약 pill 버튼·알림 벨(카운트 배지)·다크모드 토글·사용자 아바타+이름+직함
- **좌측 사이드바** (고정폭): 섹션 레이블 아래 아이콘+텍스트 내비게이션 항목들(현재 메뉴는 연한 배경으로 활성 표시), 하단에 "즐겨찾기" 고정 항목 블록, 최하단에 메뉴 편집 링크 + 연결 상태(서버·버전) 표시줄
- **메인 콘텐츠**: 상단 인사말 헤더(오늘 날짜 + 주요 액션 버튼) → 핵심 지표 카드 4개(아이콘+레이블+큰 숫자+보조 텍스트, 헤어라인 테두리 카드) → 3단 워크스페이스 그리드(좌: 오늘 목록, 중앙: 선택 항목 상세 패널(탭 구조), 우: 관련 오더·최근 활동 리스트)
- **우측 AI 패널** (접이식, 톤 다른 배경으로 구분): 빠른 실행 카드 리스트 + 인사이트 카드(그래프 포함) + AI 결과 면책 문구
- **상태 표시**는 색이 채워진 배경 라벨이 아니라 **작은 점(dot) + 텍스트**로 표현한다 (`docs/DESIGN.md`의 배지 규칙과 일치).

새 화면을 설계할 때 이 골격에서 벗어나는 구조 변경(컬럼 수, 사이드바 위치, AI 패널 유무 등)이 필요하면 임의로 바꾸지 말고 먼저 사용자에게 확인한다.
